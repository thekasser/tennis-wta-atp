{
  "name": "tennis-wta-atp-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker serving the tennis dashboard + /api/* read endpoints backed by D1.",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript":                "^5.5.0",
    "wrangler":                  "^3.78.0"
  }
}
